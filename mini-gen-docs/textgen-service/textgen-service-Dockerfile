# Start from an official, small Python image.
FROM python:3.11-slim

# Create and switch to the application directory inside the container.
WORKDIR /app

# Copy the dependencies file.
COPY requirements.txt .
# Install Python dependencies (Flask, Groq SDK).
RUN pip install --no-cache-dir -r requirements.txt

# Copy the main API application file.
COPY app.py .

# Inform Docker that the application runs on internal port 5001.
# This port is exposed internally by docker-compose.yml.
EXPOSE 5001

# Set an environment variable for Flask to run in production mode.
ENV FLASK_ENV=production
# The final command to run when the container starts.
CMD ["python", "app.py"]
